---
title: Watching
layout: layouts/base.njk
pagination:
  data: collections.movie
  size: 50
  alias: posts
  reverse: true
---


  <h2 class="title">{{ title }}</h2>

  <p> This is a collection of movies I watched recently. Some of them have short comments, observations, notes, and some only have star ratings.</p>


    <ul class="posts">
      {% for post in posts %}
      <li>
        <a href="{{post.url}}">
          {{post.data.title}}
          {% if post.data.rewatched %} 🔁 {% endif %}
          <div class="tags">
            {{post.data.author}}
          </div>
        </a>
      </li>
      {% endfor %}
    </ul>
    
{# <p>
{% if pagination.href.previous %}
<a href=" {{ pagination.href.previous }}">Previous</a>
{% else %}
Previous
{% endif %} 
/ 
{% if pagination.href.next %}
<a href=" {{ pagination.href.next }}">Next</a>
{% else %}
Next
{% endif %}
</p> #}