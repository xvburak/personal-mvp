---
title: Archive
layout: layouts/base.njk
pagination:
  data: collections.book
  size: 10
  alias: posts
  reverse: true
---

  <h2>Reading</h2>

  <ul class="posts">
      {% for post in posts | reverse %}
        <li>
          <a href="{{post.url}}">
            {{post.data.title}}
            <div class="tags">
                {% for i in range(post.data.stars) %}
                    ★
                {% endfor %}
            </div>
          </a>
        </li>
        {% endfor %}
    </ul>

    
{# <p>
{% if pagination.href.previous %}
<a href=" {{ pagination.href.previous }}">Previous</a>
{% else %}
Previous
{% endif %} 
/ 
{% if pagination.href.next %}
<a href=" {{ pagination.href.next }}">Next</a>
{% else %}
Next
{% endif %}
</p> #}